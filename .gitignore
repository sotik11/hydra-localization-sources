node_modules/
*.log

# secrets — Steam Web API key, etc.
.env
.env.local

# local run backups / raw dumps — not source
data/*.prev.json
data/*.bak.json
data/*.full-*.json
data/*.json.backup

# manual overrides research (candidates, cached app list — huge, regenerated on demand)
data/*.candidates.json
data/steam-applist.json
data/steam-applist.json.gz
data/*.backup
